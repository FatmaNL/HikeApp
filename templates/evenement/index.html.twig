<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
<table border="1">
    <tr>
        <td>depart</td>
        <td>destination</td>
        <td>nombre participants</td>
        <td>Date</td>
        <td>Durée</td>
        <td>prix</td>
        <td>programme</td>
        <td>contact</td>
        <td>informations</td>
        <td>type</td>
        <td>circuit</td>
        <td></td>
        <td></td>

    </tr>

    <tr {% for e in evenement %}>
        <td>{{ e.depart }}</td>
        <td>{{ e.destination }}</td>
        <td>{{ e.nbparticipant }}</td>
        <td>{{ e.dateevenement| date('Y-m-d') }}</td>
        <td>{{ e.duree }}</td>
        <td>{{ e.prix }}</td>
        <td>{{ e.programme }}</td>
        <td>{{ e.contact }}</td>
        <td>{{ e.infos }}</td>
        <td>{{ e.type }}</td>
        <td>{{ e.circuit }}</td>
        <td><a href="{{ path ('updateevent',{'id':e.id}) }}">Modifier</a></td>
        <td><a href="{{ path ('delete',{'id':e.id}) }}">Supprimer</a></td>
    </tr {% endfor %}>

    <td><a href="{{ path ('addevent') }}">Ajouter un evenement</a></td>

</table>

<form method="post" action="{{ path('search') }}">
    <label>Recherche</label>
    <input type="text" name="search"><input type="submit" value="Eecherche">
</form>

</body>
</html>
